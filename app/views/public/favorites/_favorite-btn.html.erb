<% if recipe.favorited_by?(current_user) %>
  <p>
    <%= link_to public_recipe_favorites_path(recipe), method: :delete, remote: true do %>
      ♥<%= recipe.favorites.published.count %> 作ってみたい！
    <% end %>
  </p>
<% else %>
  <p>
    <%= link_to public_recipe_favorites_path(recipe), method: :post, remote: true do %>
      ♡<%= recipe.favorites.published.count %> 作ってみたい！
    <% end %>
  </p>
<% end %>